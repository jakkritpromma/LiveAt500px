<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rabbidcompany.liveat500px">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- If no theme stated, the default or above theme will be used. -->
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="32b970014192a247b828de185fe0bb7a14c960f2" />

        <!--
            Bind this with glide.
             <meta-data
            android:name="com.rabbidcompany.liveat500px.configuration.GlideConfiguration"
            android:value="GlideModule" />
        -->

        <activity android:name=".activity.MoreInfoActivity"
            android:theme="@style/AppThemeWithActionBar"></activity>
    </application>

</manifest>